services:
  part-1c:
    build: .
    command: npm start 
    ports:
      - 3001:3000
    volumes:
      - .:/usr/src/app/
      # - node_modules:/usr/src/app/node_modules 
    container_name: react-app-part-1c

# volumes:
#   node_modules:  